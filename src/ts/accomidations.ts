export function accomidations(element: HTMLDivElement) {
  element.innerHTML = `
    <div>
      <h3>Accomidations</h3>
      <p>places to stay will go here. probably a list of them, not sure. maybe a map too?</p>
    </div>
`;
}
